import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: 300,
    padding:10,
    marginTop:10,
    color: "white",
    backgroundColor: "gray",
    marginLeft: "auto",
    marginRight: "auto",
    borderWidth: 1,
    borderColor: "black",
    borderRadius: 10,
  },
});
